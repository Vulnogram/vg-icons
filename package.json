{
  "name": "vg-icons",
  "version": "0.0.1",
  "description": "SVG icon set and CSS",
  "main": "index.js",
  "scripts": {
    "dist": "rm -f svg/*.svg && svgo -f src/ -o svg/ --multipass -p 3 && mkdir -p dist && node index.js"
  },
  "author": "Chandan B.N.",
  "license": "MIT",
  "dependencies": {
    "svgo": "^1.3.2"
  }
}
